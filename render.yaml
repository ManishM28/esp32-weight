services:
  - type: web
    name: esp32-weight
    runtime: python
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: gunicorn app:app
